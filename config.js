// Pacelog config — paste your Supabase credentials here.
// This file is loaded by every device that visits the app, so you only
// configure it once (on GitHub) and all devices sync automatically.
//
// To edit on GitHub: click this file in your repo → pencil icon → edit → Commit.
// Pages redeploys in ~60s.

window.PACELOG_CONFIG = {
  supabaseUrl: "",   // e.g. "https://xxxxxxxxxxxxxxxx.supabase.co"
  supabaseKey: "",   // the anon public key (long eyJ... string)
};
